# Telegram Travel Assistant MVP - Minimal Dependencies
# Simplified for serverless deployment on Vercel

# Core AI service (Gemini via Vertex AI - New unified SDK)
google-genai>=0.2.0
pypdfium2>=4.30.0

# Database (Supabase PostgreSQL)
supabase==2.1.0

# Configuration management
python-dotenv==1.0.0

# Image processing (for receipt/ticket OCR)
Pillow==10.0.0
